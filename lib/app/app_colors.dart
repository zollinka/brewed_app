import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Colors.amber;
  static const Color background = Colors.white;
  static const Color accent = Color(0xFFFD9001);
  static const Color lightaccent = Color(0xFFffd399);
  static const Color textBody = Color(0xFF453b27);
  static const Color textAccent = Color(0xFFCD5000);
  static const Color disabled = Color(0xFF453b27);
}